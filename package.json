{
  "name": "appinfo-api",
  "version": "1.0.0",
  "description": "API for retrieving app information from iTunes",
  "main": "index.js",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "vercel": "^32.5.0"
  }
}